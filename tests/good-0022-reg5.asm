	РОН5 := !П (П=1)
	РОН5 := РР - РОН5 - 1 + П (П=1)
	РОН5 := РОН5 - РР - 1 + П (П=1)
	РОН5 := РОН5 + РР + П (П=1)
	РОН5 := РР + П (П=1)
	РОН5 := !РР + П (П=1)
	РОН5 := РОН5 + П (П=1)
	РОН5 := !РОН5 + П (П=1)
	РОН5 := РОН5 and РР
	РОН5 := РОН5 xor РР
	РОН5 := !(РОН5 xor РР)
	РОН5 := !РОН5 and РР
	РОН5 := РОН5 and !РР
	РОН5 := РОН5 or !РР
	РОН5 := !РОН5 or РР
	РОН5 := РОН5 or РР
