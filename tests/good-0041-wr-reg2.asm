	РР := !П (П=0)
	РР := РР - РОН2 - 1 + П (П=0)
	РР := РОН2 - РР - 1 + П (П=0)
	РР := РОН2 + РР + П (П=0)
	РР := РР + П (П=0)
	РР := !РР + П (П=0)
	РР := РОН2 + П (П=0)
	РР := !РОН2 + П (П=0)
	РР := РОН2 and РР
	РР := РОН2 xor РР
	РР := !(РОН2 xor РР)
	РР := !РОН2 and РР
	РР := РОН2 and !РР
	РР := РОН2 or !РР
	РР := !РОН2 or РР
	РР := РОН2 or РР
