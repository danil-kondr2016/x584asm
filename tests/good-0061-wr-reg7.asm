	РР := !П (П=0)
	РР := РР - РОН7 - 1 + П (П=0)
	РР := РОН7 - РР - 1 + П (П=0)
	РР := РОН7 + РР + П (П=0)
	РР := РР + П (П=0)
	РР := !РР + П (П=0)
	РР := РОН7 + П (П=0)
	РР := !РОН7 + П (П=0)
	РР := РОН7 and РР
	РР := РОН7 xor РР
	РР := !(РОН7 xor РР)
	РР := !РОН7 and РР
	РР := РОН7 and !РР
	РР := РОН7 or !РР
	РР := !РОН7 or РР
	РР := РОН7 or РР
