	РОН5 := !П (П=0)
	РОН5 := РР - РОН5 - 1 + П (П=0)
	РОН5 := РОН5 - РР - 1 + П (П=0)
	РОН5 := РОН5 + РР + П (П=0)
	РОН5 := РР + П (П=0)
	РОН5 := !РР + П (П=0)
	РОН5 := РОН5 + П (П=0)
	РОН5 := !РОН5 + П (П=0)
