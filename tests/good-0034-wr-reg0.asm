	РР := !П (П=1)
	РР := РР - РОН0 - 1 + П (П=1)
	РР := РОН0 - РР - 1 + П (П=1)
	РР := РОН0 + РР + П (П=1)
	РР := РР + П (П=1)
	РР := !РР + П (П=1)
	РР := РОН0 + П (П=1)
	РР := !РОН0 + П (П=1)
	РР := РОН0 and РР
	РР := РОН0 xor РР
	РР := !(РОН0 xor РР)
	РР := !РОН0 and РР
	РР := РОН0 and !РР
	РР := РОН0 or !РР
	РР := !РОН0 or РР
	РР := РОН0 or РР
