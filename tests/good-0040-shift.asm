	(РР, РРР) := СЦЛ(РР - ШИНвх - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + ШИНвх + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН0 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН1 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН2 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН3 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН4 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН5 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН6 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР - РОН7 - 1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН0 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН1 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН2 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН3 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН4 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН5 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН6 + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + РОН7 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + П, РРР) (П=0)
	(РР, РРР) := САП(РР - ШИНвх - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + ШИНвх + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН0 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН1 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН2 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН3 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН4 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН5 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН6 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР - РОН7 - 1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН0 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН1 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН2 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН3 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН4 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН5 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН6 + П, РРР) (П=0)
	(РР, РРР) := САП(РР + РОН7 + П, РРР) (П=0)
	РР := САП(РР + П) (П=0)
	РР := СЦП(РР + П) (П=0)
	РР := САЛ(РР + П) (П=0)
	РР := СЦЛ(РР + П) (П=0)
	РР := СЛП(РР + П) (П=0)
	РР := СЛЛ(РР + П) (П=0)
	(РР, РРР) := СЦП(РР + П, РРР) (П=0)
	(РР, РРР) := САЛ(РР + П, РРР) (П=0)
	(РР, РРР) := СЦЛ(РР + П, РРР) (П=0)
	(РР, РРР) := СЛП(РР + П, РРР) (П=0)
	(РР, РРР) := СЛЛ(РР + П, РРР) (П=0)
