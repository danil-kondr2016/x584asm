	РР := !П (П=1)
	РР := РР - РОН1 - 1 + П (П=1)
	РР := РОН1 - РР - 1 + П (П=1)
	РР := РОН1 + РР + П (П=1)
	РР := РР + П (П=1)
	РР := !РР + П (П=1)
	РР := РОН1 + П (П=1)
	РР := !РОН1 + П (П=1)
	РР := РОН1 and РР
	РР := РОН1 xor РР
	РР := !(РОН1 xor РР)
	РР := !РОН1 and РР
	РР := РОН1 and !РР
	РР := РОН1 or !РР
	РР := !РОН1 or РР
	РР := РОН1 or РР
