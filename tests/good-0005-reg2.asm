РОН2 := !П (П=0);
РОН2 := РР - РОН2 - 1 + П (П=0);
РОН2 := РОН2 - РР - 1 + П (П=0);
РОН2 := РОН2 + РР + П (П=0);
РОН2 := РР + П (П=0);
РОН2 := !РР + П (П=0);
РОН2 := РОН2 + П (П=0);
РОН2 := !РОН2 + П (П=0);
