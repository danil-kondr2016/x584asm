	РР := !П (П=0)
	РР := РР - РОН5 - 1 + П (П=0)
	РР := РОН5 - РР - 1 + П (П=0)
	РР := РОН5 + РР + П (П=0)
	РР := РР + П (П=0)
	РР := !РР + П (П=0)
	РР := РОН5 + П (П=0)
	РР := !РОН5 + П (П=0)
	РР := РОН5 and РР
	РР := РОН5 xor РР
	РР := !(РОН5 xor РР)
	РР := !РОН5 and РР
	РР := РОН5 and !РР
	РР := РОН5 or !РР
	РР := !РОН5 or РР
	РР := РОН5 or РР
