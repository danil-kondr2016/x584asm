	РОН1 := !П (П=1)
	РОН1 := РР - РОН1 - 1 + П (П=1)
	РОН1 := РОН1 - РР - 1 + П (П=1)
	РОН1 := РОН1 + РР + П (П=1)
	РОН1 := РР + П (П=1)
	РОН1 := !РР + П (П=1)
	РОН1 := РОН1 + П (П=1)
	РОН1 := !РОН1 + П (П=1)
	РОН1 := РОН1 and РР
	РОН1 := РОН1 xor РР
	РОН1 := !(РОН1 xor РР)
	РОН1 := !РОН1 and РР
	РОН1 := РОН1 and !РР
	РОН1 := РОН1 or !РР
	РОН1 := !РОН1 or РР
	РОН1 := РОН1 or РР
