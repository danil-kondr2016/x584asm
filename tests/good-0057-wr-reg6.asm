	РР := !П (П=0)
	РР := РР - РОН6 - 1 + П (П=0)
	РР := РОН6 - РР - 1 + П (П=0)
	РР := РОН6 + РР + П (П=0)
	РР := РР + П (П=0)
	РР := !РР + П (П=0)
	РР := РОН6 + П (П=0)
	РР := !РОН6 + П (П=0)
	РР := РОН6 and РР
	РР := РОН6 xor РР
	РР := !(РОН6 xor РР)
	РР := !РОН6 and РР
	РР := РОН6 and !РР
	РР := РОН6 or !РР
	РР := !РОН6 or РР
	РР := РОН6 or РР
