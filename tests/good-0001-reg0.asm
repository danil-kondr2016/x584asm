	РОН0 := !П (П=0)
	РОН0 := РР - РОН0 - 1 + П (П=0)
	РОН0 := РОН0 - РР - 1 + П (П=0)
	РОН0 := РОН0 + РР + П (П=0)
	РОН0 := РР + П (П=0)
	РОН0 := !РР + П (П=0)
	РОН0 := РОН0 + П (П=0)
	РОН0 := !РОН0 + П (П=0)
	РОН0 := РОН0 and РР
	РОН0 := РОН0 xor РР
	РОН0 := !(РОН0 xor РР)
	РОН0 := !РОН0 and РР
	РОН0 := РОН0 and !РР
	РОН0 := РОН0 or !РР
	РОН0 := !РОН0 or РР
	РОН0 := РОН0 or РР
